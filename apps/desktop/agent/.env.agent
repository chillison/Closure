# Tracked dev env for the agent service. Loaded via `--env-file=.env.agent`
# in the dev script. Uses port 18422 to avoid conflicting with 4001.
# For local-only overrides, create `.env.agent.local` (gitignored).
PORT=18422
