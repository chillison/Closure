# Orison Space — Dark Slots Override
# 仅列出在暗色主题下需要不同映射的槽位
# 未列出的槽位沿用 slots.yaml 中的引用（会自动解析到 dark palette）

# 示例：如果暗色下 agent 面板需要比侧边栏更深
# agent-panel-bg: "{surface}"

# 细弱分隔边框：暗色下改用浅色低透明度，避免暖灰在深背景上不可见
border-faint: "rgba(255, 255, 255, 0.08)"
