lint:
  # ── Toolbar ──
  scan: "Scan manuscript"
  scanning: "Scanning…"
  classify: "Contextual judgment"
  classifying: "Judging…"
  classifyDisabledNoModel: "No model configured — enable a model in Model Settings first (auto-picked when the review-judge slot is unset)"
  classifyDisabledNoReport: "Run a full scan first"
  degraded: "Contextual judgment degraded (no model / call failed) — static results remain fully usable"
  reportAt: "Report at {time}"
  # ── Severity filter ──
  severityAll: "All"
  severityHigh: "High"
  severityMedium: "Medium"
  severityLow: "Low"
  severityEmpty: "No findings at this level"
  issuesTitle: "Issues ({n})"
  densityTitle: "Density fingerprints ({n})"
  densityRow: "{hits} hits ({perKilo}/k chars)"
  emptyReport: "No scan yet — click \"Scan manuscript\" to start."
  noProject: "Open a project first."
  # ── Scan coverage (batch-A field consumption) ──
  skippedSummary: "Skipped {n} chapters (not scanned): {reasons}"
  degradedChapters: "{n} chapters scanned in degraded mode (engine failure; results may be incomplete): {chapters}"
  classifyPartial: "Contextual judgment partially covered (some rule groups unjudged)"
  # ── Issue rows ──
  locate: "{chapter} line {line}"
  verdictBadge: "True positive {ratio}%"
  groupCount: "×{n}"
  suggestReplace: "Suggest: change to \"{text}\""
  suggestDelete: "Suggest: delete"
  truncated: "Truncated — {n} findings in total, showing the first 300; see the full diagnostic report for filtering and export."
  openFailed: "Failed to open the chapter file"
  # ── Mechanical fixes ──
  fixesTitle: "Mechanical fixes (fixability:auto)"
  fixPatchCount: "{n} patches ({del} deletions)"
  fixDelete: "Delete"
  fixReplace: "Replace"
  noFixes: "No mechanically fixable findings in this scan."
  applyFix: "Apply fixes"
  applying: "Applying…"
  noFixSelection: "Select at least one chapter to fix."
  applySuccess: "Fixed {n} chapters."
  confirmTitle: "Apply mechanical fixes"
  confirmBody: "The following chapters' prose will be modified (deterministic replacements, no LLM):"
  confirmChapter: "{chapter}: {n} patch(es)"
  confirmDeletions: " ({n} deletions — matched text removed outright)"
  confirm: "Apply"
  cancel: "Cancel"
  # ── Error codes (pattern A) ──
  errorNoProject: "Project path missing."
  errorProjectNotFound: "Project file unreadable (corrupt or missing) — reopen the project."
  errorEngine: "Lint rulesets failed to load — retry or reinstall the app."
  errorInvalidPatches: "Invalid fix patch payload — rescan and retry."
  errorOperation: "Operation failed: {reason}"
